# uclogic-libwacom-tablet-descriptions

Use me to create basic libwacom tablet description files.

```
$ npm install
$ npm start
```

Edit index.js invocation to `main` function to download different drivers.